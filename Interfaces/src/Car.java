public interface Car { //abstract is also implied
    int maxDist (int tankVolume); // public abstract method - no body (implied)
    int GVM = 2000; //constant / final (public static final implied)
}

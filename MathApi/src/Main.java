public class Main {
    public static void main(String[] args){
        long a = 8;
        int b = 6;
        int c = Math.min((int)a, b);  // careful with implicit casting

        //round() - takes decimal and returns an integral number
            //float parameter returns int
            //double parameter returns long
            //cast explicitly where needed
        //ceil(), floor(), pow() - always returns double
        //random() - returns double (0-1.0)
    }
}
